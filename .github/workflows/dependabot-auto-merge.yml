---
name: Dependabot auto-merge
on:
  pull_request_target:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
  workflow_call:
    inputs:
      unconditional:
        required: false
        type: boolean
        description: Whether to unconditionally approve PRs without checking the update type
        default: false
      auto-merge-after-requirements:
        required: false
        type: boolean
        description: Whether to enable auto-merge after branch protection requirements are met
        default: true
      runs-on:
        required: false
        type: string
        description: Runner to use
        default: ubuntu-slim
    secrets:
      GH_TOKEN:
        required: false
        description: GitHub token
permissions:
  contents: write
  pull-requests: write
  actions: read
  checks: read
  statuses: read
defaults:
  run:
    shell: bash -euo pipefail {0}
    working-directory: .
jobs:
  dependabot-auto-merge:
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    runs-on: ${{ inputs.runs-on || 'ubuntu-slim' }}
    env:
      PR_URL: ${{ github.event.pull_request.html_url }}
      GH_TOKEN: ${{ secrets.GH_TOKEN || github.token }}  # zizmor: ignore[secrets-outside-env] caller-provided secret
    steps:
      - name: Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@25dd0e34f4fe68f24cc83900b1fe3fe149efef98  # v3.1.0
        with:
          github-token: ${{ env.GH_TOKEN }}
      - name: Verify a safe Dependabot update type
        id: safe-update
        env:
          PACKAGE_ECOSYSTEM: ${{ steps.metadata.outputs.package-ecosystem }}
          UPDATE_TYPE: ${{ steps.metadata.outputs.update-type }}
        run: |
          safe=false
          case "${UPDATE_TYPE}" in
            version-update:semver-patch)
              safe=true
              ;;
            version-update:semver-minor)
              if [[ "${PACKAGE_ECOSYSTEM}" != "github_actions" ]]; then
                safe=true
              fi
              ;;
          esac
          echo "safe=${safe}" | tee -a "${GITHUB_OUTPUT}"
      - name: Approve the pull request
        if: >
          github.event_name == 'pull_request_target'
          || inputs.unconditional
          || steps.safe-update.outputs.safe == 'true'
        id: approve-pr
        run: >
          gh pr review --approve "${PR_URL}"
      - name: Check whether the PR head branch is behind the base
        id: merge-state
        if: >
          steps.approve-pr.outcome == 'success'
          && (github.event_name == 'pull_request_target' || inputs.auto-merge-after-requirements)
        run: |
          merge_state="$(gh pr view "${PR_URL}" --json mergeStateStatus --jq '.mergeStateStatus')"
          case "${merge_state}" in
            BEHIND|BLOCKED|CLEAN|DIRTY|DRAFT|HAS_HOOKS|UNKNOWN|UNSTABLE)
              ;;
            *)
              echo "Unexpected mergeStateStatus: ${merge_state}" >&2
              exit 1
              ;;
          esac
          echo "status=${merge_state}" | tee -a "${GITHUB_OUTPUT}"
      - name: Request Dependabot rebase if the PR head branch is behind the base
        if: >
          (github.event_name == 'pull_request_target' || inputs.auto-merge-after-requirements)
          && steps.merge-state.outcome == 'success'
          && steps.merge-state.outputs.status == 'BEHIND'
        run: >
          gh pr comment "${PR_URL}" --body "@dependabot rebase"
      - name: Verify required checks if configured
        if: >
          (github.event_name == 'pull_request_target' || inputs.auto-merge-after-requirements)
          && steps.merge-state.outcome == 'success'
          && (steps.merge-state.outputs.status == 'CLEAN' || steps.merge-state.outputs.status == 'UNSTABLE')
        run: |
          if ! check_output="$(gh pr checks --required "${PR_URL}" 2>&1)"; then
            # Work around gh CLI behavior where --required exits non-zero
            # when the PR branch has no required checks.
            if grep -Eq "no (required )?checks reported" <<<"${check_output}"; then
              echo "${check_output}"
              echo "No required checks are configured for this pull request; skipping required-check verification."
            else
              echo "${check_output}" >&2
              exit 1
            fi
          fi
      - name: Enable auto-merge after necessary requirements are met
        if: >
          (github.event_name == 'pull_request_target' || inputs.auto-merge-after-requirements)
          && steps.merge-state.outcome == 'success'
          && (steps.merge-state.outputs.status == 'CLEAN' || steps.merge-state.outputs.status == 'UNSTABLE')
        run: >
          gh pr merge --merge --delete-branch --auto "${PR_URL}"
